import { AppContainer, Container } from "@/components/layout/Containers";
import { Title, Text } from "@/components/ui";

export function Component() {
  return (
    <AppContainer>
      <Container>
        <Title>Autenticação JWT & RS256</Title>
        <Text>Fluxo de autenticação moderno e seguro.</Text>
      </Container>
    </AppContainer>
  );
}
